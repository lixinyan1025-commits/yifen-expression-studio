# 验证记录

日期：2026-09-08。项目：`C:\Users\36030\Documents\ChatGPT\演讲`。

## 2026-09-11 GitHub Pages 手机入口

- 新增 GitHub Pages 构建模式，资源基址固定为 `/yifen-expression-studio/`；README 顶部提供醒目的手机版入口与红色打开按钮。
- Pages 版使用 GitHub Actions Secret 中的 SHA-256 值完成浏览器端密码校验，明文密码不进入源码或构建产物。该门槛不能代替服务端权限，因此完整后端版继续使用 PBKDF2 与签名 Cookie。
- 390 px Chrome 实测密码页可见、错误密码被拒绝、正确密码解锁并保存 12 小时状态；页面无横向溢出、运行错误为 0。Pages 版保留学习、计时、录音、回听、导入和浏览器历史，不声称支持其静态托管无法运行的转写与 AI 分析。
- 核心与 Worker 测试 20 / 20 通过，`npm.cmd run build:pages` 通过，生成资源链接均包含仓库子路径。

## 2026-09-11 密码保护的公共访问

- 在线页面改为公网可打开、输入站点密码后进入；密码仅以 PBKDF2-SHA256 哈希保存在 Sites 加密环境变量中，不进入前端、源码仓库或浏览器存储。
- 登录成功签发 12 小时 HttpOnly、Secure、SameSite=Strict 签名 Cookie；退出按钮立即清除 Cookie。所有 `/api/` 业务接口均在 Worker 端校验会话，并继续执行同源、自定义请求头和请求体大小检查。
- 新增正确密码、错误密码、跨站登录、未登录 API、退出清除会话与携带会话的真实 Worker 请求测试。生产访问验证以本次部署结果为准。

## 2026-09-09 手机优先与私人云端适配

- 话题改为独立于知识库的 12 个常见场景，学习展示三条简短中立背景信息，不再展示整篇笔记；没有笔记也可默认先学习再演讲。既有“按笔记匹配出题 / 默认阅读原始笔记”的记录仅描述此前版本。
- 核心与 HTTP / Worker 测试 **20 / 20** 通过。新增云端身份头检查、跨站拦截、流式请求体限额、云端不读取本机路径、状态不泄露密钥，以及同一 WAV 的转写 / 分析端到端测试。
- 浏览器整轮 13 / 14 通过，剩余麦克风拒绝场景因测试内写死旧端口而超时；改为跟随测试环境、隔离真实知识库后，单独复跑通过，累计 14 项均通过。其他场景验证了空知识库开始练习、3 条背景信息、资料隐藏与 AI 出题请求不含个人标题、标签、正文。
- 云端产物在本机测试网关下实际录制约 7.32 秒合成音源，生成 WAV 并定位停顿片段回听，页面无运行错误。测试网关仅在 `.local` 下，身份模拟代码不参与部署。
- 手机优先界面已截图检查；320 / 390 / 430 / 768 / 1440 px 无横向溢出。底部导航、较大触控按钮与 16 px 背景正文便于手机阅读。
- Sites 已创建，访问配置为 custom，允许用户仅站点所有者一人，无组、协作者或外部访客。生产发布结果和匿名访问检查以本次实际发布记录为准。
- 没有上传本机 `.env`、60 篇智慧原文、历史录音或真实语音；未配置真实 STT / AI 密钥，不声称已验证真实中文识别质量。

## 2026-09-09 访问恢复与使用体验优化

- 访问失败时，4317 端口无监听，HTTP 请求连接失败；系统记录当天 07:52 重启。恢复本地服务后，主页与状态接口可访问。
- 新增 `打开表达练习室.cmd` / `npm.cmd run open`：通过启动器从无监听状态启动生产服务；第二次运行复用相同进程（本次 PID 2520），健康检查返回 `appId: yifen-expression`。没有安装开机自启，重启电脑后需要再次打开启动文件。
- 核心与 HTTP 测试 **17 / 17** 通过；浏览器回归 **12 / 12** 通过。包含真实 MediaRecorder 采集合成音源、片段定位回听、测试服务分析失败重试和同题比较，以及新增偏好保存、学习暂停 / 继续 / 退出、手机知识库筛选和断线恢复场景。
- 暂停测试使用可控浏览器时钟：学习 5 秒剩余 09:55，暂停后跨过 10 分钟仍为 09:55，继续 1 秒后为 09:54。上传授权在刷新后仍默认关闭。
- 真实 Obsidian 目录再次逐篇核对 **60 / 60** 原始文件一致，自动选择 3 篇正文笔记、避开入口页；手动同步和浏览器刷新后保持 60 篇。
- 当前生产页面在 320 / 390 / 768 / 1440 px 检查无横向溢出，页面错误 0，外部请求 0。已实际打开检查桌面首页、手机真实知识库和手机暂停学习截图；文件位于 `.local/optimized-*.png`。
- TypeScript / Vite 生产构建和 Prettier 检查通过。录音机制未改动，此轮未重复此前已通过的真实 60 秒等待测试。
- **真实 STT / AI 服务仍未配置。**上述测试不代表已经验证真实中文转写和语义分析准确性；本次没有把真实智慧笔记或用户音频发送给外部服务。

## Obsidian 直连追加验证

- 从 `%APPDATA%/obsidian/obsidian.json` 核实当前打开的是 `D:\obsidianbiji\obsidianbiji\obsidianbiji\obsidianbiji`，只连接其中 `01-智慧`，没有连接工作区中的笔记副本。
- 真实本机接口读取 **60 篇**：智慧入口 1 篇、入世子目录 28 篇、出世子目录 31 篇。其中按文件名识别入口页 3 篇。
- 逐篇对比接口原始 Markdown 与磁盘文件，60 / 60 一致；浏览器自动载入、手动同步和刷新后数量均为 60，稳定 ID 避免重复。
- 真实浏览器自动选择 3 篇学习材料，均未选择入口 / 索引页。390 px 直连页面没有横向溢出；外部请求 0，页面错误 0。
- 直连改动后核心测试 **17 / 17** 通过，新增递归读取、稳定 ID、只读校验、忽略目录联接越界、无效配置测试。
- 直连改动后浏览器回归 **8 / 8** 通过（7 个原有场景，加上直连自动载入、缓存更新和删除、保留手动笔记场景）。此前已通过的真实 60 秒停止测试未重复等待，录音机制没有变化。
- `.env` 已配置真实目录并验证被 Git 忽略。真实语音 / AI 密钥依然未配置；直连不触发外部上传。
- 后续内容修改通过打开网站或「同步更新」读取；不是文件系统实时监听或双向写入。

## 已通过

- `npm.cmd run build`：TypeScript 与 Vite 生产构建通过。应用、React 和依赖拆分为三个脚本包；单包均低于 500 kB。Zod 依赖包含不影响构建的纯注释解析提示。
- `npm.cmd test`：14 / 14 通过。
- 浏览器 8 个场景均已验证通过。最后整轮 7 / 8 通过，剩余项为测试文本定位同时匹配可见正文和折叠源文件；缩小定位范围后单独复跑通过，未修改产品逻辑。不重复运行已通过的 60 秒等待测试。
- 本机 Chrome 生产构建检查：主页、知识库正常渲染，1440 px 桌面和 390 / 320 px 窄屏无横向溢出；页面运行错误 0；外部请求 0。
- 依赖安装审计：0 个已知漏洞（安装时 npm 审计结果）。
- 桌面与手机截图已实际打开检查，保存在 Git 忽略的 `.local/production-desktop.png`、`.local/production-mobile.png`。测试报告截图中明确显示“测试夹具”，不作为真实分析示例。

## 核心单元与 HTTP 适配验证

1. YAML 标题、中文和分层标签、Markdown 原文保持；错误 YAML 明确拒绝。
2. 内置现实话题使用本地来源标记，不直接复制笔记标题。
3. PCM WAV 转换保持采样时间轴；合成音源中 1.2–3.2 秒的静音区间按 20 ms 帧准确检测，阈值调高后排除。
4. 全静音、截断 WAV、非法采样参数、空转写和越界时间戳明确失败。
5. 真实本机 HTTP 请求将相同 WAV 字节发送给转写 / 音频模型接口；SHA-256 与本地录音一致。测试服务验证请求包含 multipart 时间戳参数，以及 `input_audio` WAV 字节。
6. 未配置服务、上游 HTTP 503、录音和转写哈希不一致均返回明确错误。
7. 虚构引文、无依据停顿过滤；整体评价的引文不匹配时拒绝报告。
8. 重叠 / 传递重叠的思路中断合并计数；其他问题不能借用无关停顿时间戳。
9. 跨站 Origin、缺少自定义请求头不能调用付费 API；状态接口不返回密钥。

## 浏览器场景

1. 导入 → 学习 → 临场题目隐藏 → 揭晓后资料隐藏 → 真实 MediaRecorder 录制合成音源 → 停顿定位回听 → 刷新后 IndexedDB 恢复 → 同题再练 → 删除。
2. 明确标注的测试服务响应：真实录音送至转写 / 分析路径；分析失败保留转写；重试成功后显示测试报告；段落与问题点击回听；疑似确认 / 忽略 / 恢复；第二次录音与上次比较。
3. 准备模式先展示题目；390 px 手机阅读页面无横向溢出。
4. 独立浏览器权限上下文真正拒绝麦克风，出现中文处理说明和重试按钮，不产生录音或报告。此项没有使用自动允许权限的浏览器启动参数。
5. **真实等待 60 秒**，MediaRecorder 自动结束；解码音频长度断言在 59–62 秒间，所有观察到的麦克风轨道 `readyState === ended`。
6. 十分钟学习使用可控浏览器时钟跨过到期点，验证按实际截止时间切换题目；此项没有实际等待十分钟。
7. 导入含 HTML 脚本和远程图片的测试 Markdown：脚本不执行、远程图片不请求；320 px 窄屏无横向溢出。
8. 空白首页不伪造历史记录；未配置的服务状态准确显示；没有产生上传请求。

## 未验证与现实限制

- **没有配置真实 STT / AI 密钥，没有向外部服务提交用户笔记或录音。**音频采集机制使用合成音源；所有自动分析内容来自明确标记的测试服务。不能据此声称已验证真实中文识别准确率、口头禅召回率或模型建议质量。
- 真实麦克风设备、口音、环境噪声、服务账号权限与延迟需要用户配置后验证。保留不流畅表达的提示不保证 ASR 一定逐字保留；原始音频和转写都可下载。
- 时间定位精度为服务提供的分段，不伪造逐词时间。一个粗分段中的多次相同表达可能被保守合并，次数是待人工核验的事件数。
- RMS 停顿是低音量检测，不能单独识别思路中断；背景噪声会影响结果。
- Web Audio 转码与 MediaRecorder 在 Chrome 验证；Safari / iOS 实机尚未验证。网站当前仅监听本机，手机实际访问和录音还需要 HTTPS 部署方案。
- 数据仅在浏览器当前 origin，清除网站数据可能删除记录；持续存储申请不替代备份。

## 配置后的人工验收

1. 用自己的 1–3 篇笔记进行临场训练，确认出题自然且未提前暴露提示。
2. 录制一段含自然思考停顿、一个正常语义的“那个”、一个填充词、一次自我修正与一句完整理由的中文演讲。
3. 确认分析请求成功后，逐条点击转写、停顿和问题，核对听到的片段与引文，特别检查正常语义和必要强调没有被武断判错。
4. 对疑似项逐一确认或忽略，同题再练，查看两次原文与事件变化。

在完成上述真实服务验收前，应将本项目视为已经验证本地机制与接口适配、仍待真实中文服务校准的可运行第一版。
